import {BottomPaneDirective} from './bottom-pane.directive';

describe('BottomPaneDirective', () => {
  it('should create an instance', () => {
    const directive = new BottomPaneDirective();
    expect(directive).toBeTruthy();
  });
});
